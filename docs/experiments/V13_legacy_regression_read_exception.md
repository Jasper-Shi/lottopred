# V13 prospective legacy-regression read exception

This is a separate engineering-test permission, not V13 historical execution
authority. The user explicitly authorized continuing on 2026-09-13 after the
distinction between synthetic program checks and real historical prediction
was explained: “OK，你有我的授权，你可以继续去做。” The agent first recorded
receipt at 2026-09-13T15:26:08Z; that is an observation time, not a claimed
platform timestamp for the user's message.

## Exact scope and precedence

The [machine record](../../evidence/research_registrations/v13-legacy-regression-read-exception-v1.json)
is a new, prospective user-authorized exception to the original R13 restriction
on preauthorization history/answer reads, only for the unchanged legacy tests
present at protected main `900054719a4ac089d4c059830b5c7162f2543616`.
Their source and existing production dependencies are pinned by Git metadata.
They may read only the already committed historical/data/completed-experiment
evidence their existing parsing, integrity and evidence-consistency fixtures use.
They may copy those inputs into disposable fixtures and exercise existing
synthetic fixture transformations; that does not make the real prefix synthetic.
No test output becomes a V13 forecast, score, scientific conclusion or model
selection input. Changed/new legacy readers are not authorized by this record.

This explicitly covers local full regression and automatic CI, including the
first CI run of this registration's PR before merge. Before the first such run,
freeze these proposed bytes and obtain actual independent Standards and Spec
review with zero blocker/major findings. After local checks, commit normally,
obtain exact-source CI and independent review confirmation, and merge normally
into protected main. The user's prospective permission supplies the pre-merge
test authority; this source branch supplies no historical-worker authority.

The old R13 seal remains byte-identical and retains its original meaning. It
did not already contain this exception, and its operational_contract_sha256
must not be replaced with this record's digest. This separate operational-test
schema changes no V13 scientific or execution identity. The eight reviewed I13
draft files, core SHA, science fingerprint, formulas, targets, controls, metrics,
gates and runtime contract remain unchanged. A source review is not completed
I13 acceptance, and no real V13 historical result exists at this checkpoint.

## Containment and reporting

Run the local suite in a disposable full-history checkout with an inspected Git
configuration and a scrubbed environment: no SMTP, GitHub/publication credential,
proxy, user Python path or automatic third-party pytest plugins. Permit only
local Git transport during testing. Disable tracebacks/captured-output printing
for full regression and report counts, exit status and failing node identities;
never print target payloads or use them to tune the model. These controls are
not represented as an operating-system security boundary against arbitrary
network code. Dependencies are already installed locally in a separate developer
environment; the frozen historical runtime must never be changed.

The new tests/conftest.py controls reporting only: suppress assertion tracebacks,
captured output, warning bodies and local-variable dumps while retaining failure
exit status and aggregate results. The summary prints only failing test names
with parametrized fixture suffixes removed. It changes no collection, test input, assertion or model.
The old workflow is preserved exactly because R13 binds its Git object. Its
checkout still uses the existing credential-persistence default; do not claim
that CI is credential-free. No code path may use that token for operations.

CI still needs checkout/dependency-install network access. Its unit-test body
must perform only the reviewed offline behavior. No SMTP/publication secrets,
real mail, source collection, historical worker or remote operational mutation
is authorized. Preserve the pull-request check name `test` and push check name
`push-test`; test success is program verification, not prediction performance.

V13-specific tests remain synthetic/source/math-only. Real V13 training,
prediction, reveal and scoring retain the original separate authorization and
one-shot lease requirements. No canonical attempt, capability, startup, claim,
ledger, lease, report, live/CLI/scheduler activation or mail follows from this
exception. Legacy fixture products stay in owned temporary directories; no
existing canonical artifact may be overwritten or regenerated.

## Preserved incident and corrected scope

The [prior incident record](../../evidence/research_execution_incidents/v13-legacy-test-boundary-20260913/prior-incident-record.json)
and its original log, static audit, protocol review and then-unapproved proposal
are preserved unchanged beside it. Their earlier permission-pending words are
historical statements. The root agent started a full legacy suite before
classifying its data reads, then stopped it. Exit 2, 230 passed in 668.11 seconds
is an interrupted run, not a full pass. Exact executed readers are inferred
under default ordering because the log has no node IDs. The source bytes had
already been fixed and stayed unchanged. No V13 canonical worker or forecast
was run; no formal leakage-audit conclusion is asserted. This new permission
does not retrospectively authorize that earlier test run.

The later bounded source audit also found a direct legacy CSV count/date check
at tests/test_workflow_kill_switch.py:550–573, correcting the earlier audit's
incomplete direct-reader inventory. Its two previously unexpanded YAML paths
execute only temporary guard scripts or an email script with send_email replaced
by a synthetic capture stub. They do not invoke later live/backtest/SMTP steps.
This is a source finding, not proof of a completed full-suite runtime trace.

## Subsequent Git sequence

Keep this exception in its own ordinary PR. After its protected-main merge,
create a fresh full-history implementation clone at that exact main; copy the
eight preserved I13 draft files byte-for-byte and repeat final acceptance there.
No rebase, reset, squash or force push is needed. The source diff for I13 remains
exactly eight additions; this exception is outside it. Continue the unchanged
R13 < I13 < K_H13 < A_H_s13 < M_A_H13 < L_H13 < run_H13 order only after its
real prerequisites pass. Never retry a started worker or update/delete a lease.

This registration, its fixed records and the incident files are immutable after
their first ordinary ADD. Later changes require another explicit registration.
Status documents may advance truthfully without altering these frozen records.
The broader Goal remains incomplete. The separately discussed probability and
multi-combination modules are future research planning, not a change to V13.
