# V12.0.2 Historical Operational Rebinding Pre-registration (R3)

Status at R3: **REGISTERED — NOT IMPLEMENTED — NOT AUTHORIZED — NOT SCORED**.
Registered on 2026-09-13 UTC from base
`3015078e251bdcbb92719e1b291b1807525fef16`.
Registration grants no execution authority; no future commit identity is asserted.

## Identity and bounded purpose

| Field | R3 value |
|---|---|
| Experiment | `V12_0_2_historical_operational_rebinding` |
| Scientific family / hypothesis | `V12_post_rng_parity_composition_transition` / unchanged `H12` |
| Operational version | `v12.0.2` |
| Classification | `operational_rebinding_only` |
| Statistical behavior changed | `false` |
| Statistical fingerprint | `af2e16a55ff0e817cf71208471e19e4f481bed63990f7e41268997c4c4b35c76` |
| Scope | historical only; no live lane |
| R3 execution | static registration checks only; no runtime verifier, canonical or worker invocation |
| I3 review execution | isolated synthetic/closed-form pure validators and mocked adapters/worker paths only; no production capability or canonical attempt |
| Seed / targets / halves | `649` / `627` / `314 + 313` |
| Pure-core SHA-256 | `fae93e0a6f76c6604eabe24f6b93676e22e87d7e567365b382484433fba2eb77` |

R3 registers a new operation identity to correct the exact GitHub commit
projection and retain safe durable startup evidence. It changes no candidate
law, control, score, inference, threshold, notification rule or timeout.
A fair unpredictable lottery remains the default explanation. A scientific
rejection remains an acceptable result.

R3 creates no real startup journal, claim, ledger, forecast, lease, report,
notification, credential, workflow, live release or authorization. Any later
historical diagnostic remains consumed diagnosis, never blind, confirmatory,
prospective, eligible historical-OOS evidence or promotion authority. There is
no new live canary, rescheduled old canary or R3 live activation route.

## Prior incident and uncertainty

The immutable V12.0.1
[incident record](../../evidence/research_execution_incidents/v12-0-1-20260913/startup-incident.json)
records one canonical invocation against authorization merge
`baa08ecb77555e17f383ef2998bea3c433e16f74`, exiting `1` before a persistent
claim, scientific ledger, forecast or score. The original failure stage is
unknown. Whether a lease-commit POST or createRef POST occurred is unknown:
no original nonce, expected OID, intent or response receipt was retained.
A post-invocation lease 404 is a point-in-time observation, not proof that no
POST occurred and not permission to retry.

A later read-only observation of existing source commit
`f194d5cccf30868f1088680e338fcad9cb697949` confirmed that its raw Git message
has one trailing LF while its GitHub GET JSON `message` omits that LF. The
frozen V12.0.1 verifier rejects that representation. This confirmed interface
defect on an existing source commit does not establish the original failure
stage or remote lease-object state. See the
[representation receipt](../../evidence/research_execution_incidents/v12-0-1-20260913/existing-commit-api-representation.json).

V12.0.1 stays `startup_failed_frozen_no_retry`, not scored, with no scientific
disposition. Its I2, old registrations/auth, requirements, core, lease namespace
and artifacts stay immutable. R3 neither retries nor relabels that startup as
unexecuted, Archive, Reject or consumed scientific evidence. It requires its
own later implementation, independent reviews, CI and authorization.

## Complete scientific inheritance

Scientific authority remains R1 at
`0af20fc41fc5aaa0879dada0a258797a8bc14e20`. The complete R1
[seal](../../evidence/research_registrations/v12-post-rng-parity-composition-transition-v1.json)
has SHA-256 `4406bd25ee82195bff7a97b258885cb3bb3c1a8fb829f383c5e3c1616e169170`.
All scientific clauses of the R1
[specification](V12_post_rng_parity_composition_transition.md),
[basis](../research/V12_post_rng_parity_composition_transition_basis.md) and
[configuration](../../config/research-v12-post-rng-parity-composition-transition.yaml)
are inherited at that immutable authority. Their full-file hashes are bound in
R3's `complete_R1_normative_sources`. Only the operational identities, topology,
API projection and startup receipt rules explicitly stated in R3 supersede the
old operation route. No omitted scientific detail becomes optional.

Canonical section hashes use finite sorted compact UTF-8 JSON without LF:

| R1 object | SHA-256 |
|---|---|
| `control_null_contract` | `f050c82f101a17f5126a8a8db61c7f484d83e605f27213b6803306d74fab2d8c` |
| `controls` | `cf590452b19f1dfbae0f0fbd2b1db3ce47b290c0011b2889413bdca529f271e0` |
| `gates` | `d64633c7179128d0fce76f37ead0f5740ef13e725a941794102ddc0fa00f2396` |
| `historical_governance` | `d33abcabd1bfa7548f8eab680d4efab864f52b271e111cab7212a1f0245366fc` |
| `historical_scope` | `7106f906239761694647b3f60f2ce8c5855d5b61ef8e74539b262aacf0a03a65` |
| `mathematical_contract` | `6c2391fa3d58ea1161af90282028e28d787ce52de38cb03e43366d89cf7ed832` |
| `model` | `2a97e0a6e18860c31027828302fb9230ab44bfa1b3a846663fc84c1483d66b7f` |
| `multiplicity` | `d0ce22e9e36441d9d497af56c1675f98ee618d83ea1a146a7851a7c8c5e57997` |
| `notifications` | `bb1bd1de4219d90b931b3789c33d1df4fdab36d22fb7d0ac4a2becc4eab33af2` |
| `target_date_identities` | `1264cd4fc34cd33bdfdcabba79be6efe3a6902068a17b9ed61d9ee667baccf77` |

The hashed source `model.version` stays literal `v12.0.0`; R3's operation version
is separately `v12.0.2`. Updating the inherited object to look current is drift.

H12, the pseudo partition, post-RNG boundary, prior, single coefficient,
binary64 literals, 256-step solver, ranking, Final-6 degeneracy, mean Top-12
hits minus `72/49` primary, ten conjunctive gates, 627 targets, 314/313 halves,
Holm vector, all bootstrap settings and all controls remain exact. The complete
R1 forecast-before-reveal, serialization, deduplicated opportunities, exact
Final-6 audit/stop, V12-local ledger and notification rules remain exact,
including **“历史诊断/审计候选、不可晋升”** and no recomputation on email failure.

Previously published V1 aggregate results are already known in this task.
R3 does not claim that researchers forgot them or that any historical blind
status has been restored. Here outcome-blind means only that the operational
repair is based on the fixed incident and Git metadata, with no new raw
governed-history or target-record access and no V12 forecast/score available.
Known aggregates do not select or change H12, controls, gates or this repair.
The already frozen V1 comparator and its R1 gates remain unchanged; any new
historical calculation requires the new authorization. The inherited
208-snapshot rule constrains a separately registered future prospective
proposal and grants no R3 live authority.

Governed history remains authority `4a617f2c1575a165b42878600753a01ddf2ced03`
through the registered `load_published_history` seam. Its recorded 4,444-draw
identity through 2026-08-22 is unchanged. All 2026 draws and subsequent appends
are excluded. R3 tests may inspect fixed calendar dates and Git metadata;
they may not open real history, draws, results, predictions, secrets or sources.

## New identities and exact I3 scope

| Identity | Literal |
|---|---|
| R3 registration | `evidence/research_registrations/v12-post-rng-parity-composition-transition-v3.json` |
| Research config | `config/research-v12-0-2-post-rng-parity-composition-transition.yaml` |
| Historical auth | `evidence/research_authorizations/v12-post-rng-parity-composition-transition-v3-historical.json` |
| Historical lease | `refs/heads/v12-consumption-v12.0.2` |
| Command | `python3.12 tools/run_v12_0_2_historical.py --consume-v12-0-2-once` |
| Startup journal | `reports/v12_post_rng_parity_composition_transition_v12.0.2_historical.startup.jsonl` |

Eight claim/ledger/report/staging/manifest paths retain R2's naming shape with
literal `v12.0.2`; the startup path makes nine reserved paths. The exact Final-6
bundle also uses `v12.0.2`. Never adopt, overwrite, truncate, repair, delete or
reuse an old artifact.

I3 adds exactly these six paths, each with Git status `A`, and no other file
addition, modification, deletion or rename:

```text
src/lotto649/v12_0_2_evidence.py
src/lotto649/v12_0_2_registered_attempt.py
tools/run_v12_0_2_historical.py
tests/test_v12_0_2_registered_attempt.py
tests/test_v12_0_2_evidence_equivalence.py
tests/fixtures/v12_0_2_git_commit_projection.json
```

The shared pure core already exists at its sealed SHA-256; it is not added
again. R3 itself contains registration documents, research config/seal,
registration tests and root-owned status-document synchronization, with none of
these six implementation paths.

## New authority and runtime closure

The sole future phase order is:

```text
R3 < I3 < K_H3 < A_H_s3 < M_A_H3 < L_H3 < run_H3
```

I3 needs fresh independent Standards and Spec reviews and successful `test` CI
from `github-actions` at its exact source head. Bind its source and ordinary
protected-main implementation merge. Old I2 review records cannot satisfy the
new reviews. `K_H3` is exact protected remote main containing R3 and reviewed
complete I3. `A_H_s3` has sole parent `K_H3` and adds only the new auth JSON;
it is non-authoritative. It needs its own new independent Standards/Spec
reviews and exact-head successful CI. For each source, Standards and Spec are
two independent agents with different `reviewer_agent_id` and
`review_session_id`; neither agent/session may be the corresponding source
author agent/session. The fixed source-author trailer below binds the complete
contributor list to the exact reviewed source.
Fresh reviews mean new review turns, sessions, immutable comments and exact-head
CI; old I2 or old authorization comments cannot satisfy them.

A review record has only `axis`, `check_id`, `comment_body_sha256`, `comment_id`,
`pr_number`, `publisher_login`, `review_session_id`, `reviewer_agent_id`. The
comment body has only `schema_version`, `axis`, `base_sha`, `head_sha`,
`closure_sha256`, `verdict`, `blocker_count`, `major_count`, `reviewer_kind`,
`reviewer_agent_id`, `review_session_id`, `publisher_login`. Require the exact
phase schema, `standards` or `spec`, exact source base/head and frozen closure,
`pass`, integer zero blockers/majors and `reviewer_kind=independent_agent`.

`publisher_login` reports the real GitHub account that published the comment;
it does not impersonate the reviewing agent. No different-GitHub-account
condition is introduced for historical review. This preserves the historical
I2 evidence semantics in `_verify_reviews`, including two distinct agent/session
identities, exact PR/check IDs, fixed PR issue URL, exact comment body SHA-256,
actual publisher, valid creation time and `updated_at == created_at`. The R1
live-success-source different-account condition is not copied into this
historical-only lane. Author/agent independence and publisher provenance remain
separate facts and must both validate. Each phase requires one unambiguous,
completed, successful `test` check from `github-actions` at the exact reviewed
head. New auth-source reviews are discovered from external comments/checks
after the ordinary authorization merge; they are not embedded in their own
source JSON.

Resolve A_H_s3 only from the verified ordinary M_A_H3 merge's second parent.
New auth reviews use only the fixed `https://api.github.com` GET routes below,
with full verified source SHA and positive integer IDs discovered in this flow:

```text
/repos/Jasper-Shi/lottopred/commits/{A_H_s3}/pulls?per_page=100
/repos/Jasper-Shi/lottopred/pulls/{pr_number}
/repos/Jasper-Shi/lottopred/issues/{pr_number}/comments?per_page=100
/repos/Jasper-Shi/lottopred/issues/comments/{comment_id}
/repos/Jasper-Shi/lottopred/commits/{A_H_s3}/check-runs?per_page=100
/repos/Jasper-Shi/lottopred/check-runs/{check_id}
```

Require one complete first-page collection of at most 100 items, with no next
page, incompleteness or ambiguity. Do not follow redirects, add endpoints or
pick the latest/first convenient item. Require exactly one associated merged
PR with exact source/head/base K_H3/main/merge M_A_H3 in the fixed repository,
one matching immutable review comment per axis binding that source/base/closure,
and one unambiguous exact-head test check. Refetch comments by immutable ID and
validate body/publisher/timestamps. These external facts remain outside the
auth source JSON that they later authorize.

Only ordinary reviewed merge `M_A_H3` with first parent `K_H3`, second parent
`A_H_s3` and tree equal to `A_H_s3` authorizes execution, while both protected
remote main/HEAD and the clean execution clone equal exact `M_A_H3`. No source
branch, squash, rebase, cherry-pick, stale HEAD, old auth or injected receipt
substitutes. The source JSON is self-reference-free: future source/merge OIDs
and auth review facts are discovered through verified Git topology and fresh
external attestations after they exist. No future SHA is guessed.

The runtime closure freezes at I3, containing every transitive local import
from the new runner/evidence/attempt, shared pure core, governed-history seam,
V1 factory/models, features/research features, domain, config, notification and
scientific registrations/configuration/requirements. Bind a sorted complete
path/Git-blob manifest and canonical manifest SHA-256 in new I3 reviews and the
new auth JSON. Require exact equality at **I3, K_H3, A_H_s3, M_A_H3**. Dynamic
or unresolved local imports fail closed. Separately bind all six I3 paths,
including tests and fixture. No old I2 or shared dependency bytes change.

Historical authorization needs no D0/W2/S2/C2/M_C2/K_L2/A_L2, future result or
live-canary success. R3 adds no live auth, scheduler, workflow, production
setting, factory registration, CLI subcommand, automatic invocation or refresh.
Runtime remains CPython 3.12, exact frozen requirements SHA-256
`a0dfeac17ad7e1c41dffe4b41b4810156fb028f879d312430bfc517672a570c6` and
10-second connect/30-second read GitHub timeouts. Bind exact Python patch,
platform and installed distributions afresh in A_H3 and the claim.

## Source-author provenance


Each I3 source commit and A_H_s3 source commit must contain exactly one final
trailer line with this literal ASCII prefix, including the single space:

```text
Research-Author-Provenance: <canonical compact JSON>
```

The trailer is the final nonempty line and ends in exactly one LF. There are
no bytes, blank lines or other trailers after it. The JSON itself has no LF or
CR. A preceding human-readable commit message is allowed. Find the trailer in
the raw Git commit message, not a GitHub display projection and not a caller
argument. Split raw commit headers from its message once at the first empty
line. The source commit OID naturally binds the entire raw message, including
this trailer; do not put that OID into the trailer.

Scan message lines for the exact marker `Research-Author-Provenance:`. Require
one occurrence as a line prefix, the exact single-space delimiter above, and
its final-line position. Reject additional matching markers, leading whitespace
before a marker, folded/multiline trailer JSON, missing/multiple final LF,
CRLF in the trailer, or any suffix after the trailer. Do not normalize, trim,
repair, accept another spelling or use a generic Git trailer parser that would
coalesce repeated fields. JSON bytes must equal the prescribed canonical
serialization byte-for-byte.

The JSON is a finite UTF-8 object with exactly these keys:

| Key | Exact contract |
|---|---|
| `schema_version` | `lotto649-v12-source-author-provenance-v1` |
| `phase` | `I3` on the implementation source; `A_H_s3` on the authorization source |
| `contributors` | 1–64 records, each with exactly `agent_id` and `session_id` |

Each identifier is a nonempty JSON string of at most 256 UTF-8 bytes, with no
ASCII control character (`U+0000`–`U+001F` or `U+007F`), no leading/trailing
ASCII whitespace, and no Unicode normalization or type coercion. It must be
the actual agent/session identity used during source preparation, not a
GitHub login or a newly invented identity intended to satisfy independence.
If authentic provenance is unavailable, stop rather than invent it.

Sort contributor records by `(agent_id UTF-8 bytes, session_id UTF-8 bytes)` in
ascending order. Records must be unique by the complete pair. The same agent
may truthfully appear in different authoring sessions; each such pair must be
included. Reject unsorted records, duplicate pairs, empty/out-of-range lists,
wrong types, missing/unknown keys, duplicate JSON keys at any depth, invalid
UTF-8, nonfinite values or noncanonical JSON. Canonicalization is sorted keys,
compact separators, `ensure_ascii=False`, `allow_nan=False`, UTF-8, no trailing
LF inside the JSON value.

There are no source/base/merge/OID/PR/comment/review SHA fields in this trailer.
Its exact key sets reject them. It must not include its own or a future commit
SHA, future review IDs, secrets or a path to mutable provenance. Git topology
and the existing review body supply the relevant source/base/head bindings.

### Complete authorship and independent review

List **every agent/session that contributed to the source content**, including
all contributing implementation or authorization-preparation workers and the
root/integrator's actual authoring session. This includes supplied patches,
fixtures, generated source/payload bytes, and integration edits whether or not
the contributor was the Git committer. Do not substitute only the committer,
GitHub publisher or final worker. Include another session of the same agent
when it contributed. Existing sealed dependencies do not make their historical
authors contributors to this new source; the list describes this source's work.

For A_H_s3, include everyone who prepared or changed its authorization payload
or source metadata, plus the root/integrator. Do not copy I3's list mechanically:
include those I3 contributors only when they also contributed to A_H_s3. A
person/agent who only inspected the source without changing or supplying its
content is not an author merely because they reviewed it. An agent that did
contribute source content cannot count as an independent reviewer of that
source even if it later opens a new session.

Each phase's two reviewer agent IDs must be distinct and each review session
ID must be distinct, as already required. Additionally, for **each** reviewer:

```text
reviewer_agent_id not in {c.agent_id for c in contributors}
review_session_id not in {c.session_id for c in contributors}
```

These are two separate exclusion checks; a new session does not make an author
agent independent, and a different agent does not permit reuse of an author
session ID. The review's base/head/closure, zero blocker/major counts, exact CI,
immutable comment/body hash and normal-merge requirements stay unchanged.
The real `publisher_login` remains separately authenticated GitHub publisher
provenance, not the reviewer identity, and no different-GitHub-account rule is
added.

Do not add author fields to the existing closed review-comment body. Its exact
`head_sha` already binds the source commit and therefore its author trailer.
The validator reads that same Git object's trailer and checks the existing
`reviewer_agent_id` / `review_session_id` against all contributors.

A content address proves that this declared list is bound to the reviewed
source; it cannot independently prove that the declaration is truthful or
complete. Before issuing a pass, the two independent source reviews must
cross-check the list against actual collaboration/handoff provenance, including
root integration. Uncertain, omitted or fabricated contributor identity fails
review. Reviewers report only their real identities. This completeness duty is
not replaced by successful JSON parsing.

### Fixed Git authority and no additional implementation path

For I3, read the trailer from the exact reviewed implementation source commit
already bound by the implementation merge and review `head_sha`. For A_H_s3,
read it from the exact second parent of the verified ordinary M_A_H3 merge,
matching the externally discovered authorization PR head and review `head_sha`.
Use the existing fixed local Git object reader (`git cat-file commit` at the
verified full OID); neither a worktree file nor API `message` substitutes.
Wrong source, wrong phase, absent/ambiguous trailer or malformed bytes fails
before a production capability or artifact is issued.

The trailer is source commit metadata. It leaves I3's exact six status-A paths
and A_H_s3's auth-JSON-only tree diff unchanged. It is not a seventh I3 file,
new runtime dependency, statistical feature, control or threshold. Its checks
belong inside the already registered I3 attempt/validator module and synthetic
tests. Old I2 source commits, old auth commits, old registrations, science/core
and requirements remain unchanged.

Write the final truthful trailer **before** the new source's exact-head CI and
independent reviews. Changing a contributor, session, phase or any message byte
changes the source OID; therefore previous exact-head reviews/CI no longer
satisfy authorization and must be obtained for the new head. Future auth review
facts are still discovered after the ordinary merge, with no self-reference.

## Exact raw object and fixed GET projection

Let `C` be finite canonical sorted compact UTF-8 JSON without LF. Its exact
body keys are `authorization_seal_sha256`, `canonical_command`,
`execution_authority_M_A`, `nonce_hex`, `schema_version`; the existing body
schema remains `lotto649-v12-consumption-lease-v1`.

Raw commit construction remains exact: tree equal to M_A_H3; sole parent
M_A_H3; author and committer both
`LOTTO649 V12 Consumption Lease <lotto649-v12-lease@users.noreply.github.com>`;
M_A_H3's committer instant as whole seconds at `+0000`; headers only `tree`,
`parent`, `author`, `committer`, in order; no signature/extra header; one blank
line; raw message **`C + LF`**, exactly one LF. POST `message` is also `C + LF`.

Compute local `SHA1(b"commit " + decimal_length + b"\0" + raw)` and
`SHA256(raw)`. Both POST returned OID and GET OID must equal that exact SHA-1.
GET JSON `message` must equal **`C` with no LF**. Never call `strip`/`rstrip`,
normalize whitespace, accept another message form, change raw bytes or relax
policy dynamically. GET must have the exact registered top-level/nested
structure, scalar types, tree, one parent, identities, dates and fixed-origin
OID-bound URLs. Require exact unsigned verification:

```text
verified=false; reason="unsigned";
signature=null; payload=null; verified_at=null.
```

Reject duplicate keys, nonfinite values, unknown/missing structural fields,
wrong types, dates, identity, OID, message or signature. The synthetic fixture
must implement the registered rule, never broaden it. Opaque `node_id` is typed
metadata and supplies no authority.

The following are closed schemas: every object rejects unlisted keys,
duplicate keys and wrong JSON types; only explicitly listed optional fields may
be absent. Names, email and dates in identity objects equal the exact registered
expected strings. OIDs equal the already computed expected lowercase 40-hex
identities; there is no coercion or date/URL normalization.

| Object | Required keys | Optional keys and value rule |
|---|---|---|
| Commit POST request | `tree`, `parents`, `author`, `committer`, `message` | none; parents is one string M_A_H3, identities are exact; message is C+LF; no signature key |
| Commit POST response | `sha` | only `node_id`, `url`, `html_url`, `author`, `committer`, `tree`, `message`, `parents`, `verification`; every present metadata field has the exact GET nested schema/value rule except message |
| Commit GET response | `sha`, `node_id`, `url`, `html_url`, `author`, `committer`, `tree`, `message`, `parents`, `verification` | none; message is exactly C without LF |
| Author/committer | `name`, `email`, `date` | none |
| Commit tree | `sha`, `url` | none |
| Sole parent object | `sha`, `url`, `html_url` | none; parents array has exactly this one object |
| Verification | `verified`, `reason`, `signature`, `payload`, `verified_at` | none; exact unsigned values above |
| createRef POST request | `ref`, `sha` | none; exact new lease ref and computed lease OID |
| createRef POST response and successful GET-ref reread | `ref`, `node_id`, `url`, `object` | none; exact new lease ref |
| Ref object | `sha`, `type`, `url` | none; exact lease OID and `type=commit` |

The POST response's optional `message`, if present, is required only to be a
JSON string. It is neither compared with C/raw bytes nor recorded in a receipt,
and supplies no message-identity proof. This is not an alternative accepted
GET projection: the subsequent GET remains mandatory and is the sole fixed
no-LF message check. POST's required exact OID cannot be replaced by metadata.

URL values are literal templates instantiated only with already verified OIDs:
commit API `https://api.github.com/repos/Jasper-Shi/lottopred/git/commits/{OID}`;
commit HTML `https://github.com/Jasper-Shi/lottopred/commit/{OID}`; tree API
`https://api.github.com/repos/Jasper-Shi/lottopred/git/trees/{TREE_OID}`. Parent
URLs use M_A_H3, not the lease OID. The ref URL is exactly
`https://api.github.com/repos/Jasper-Shi/lottopred/git/refs/heads/v12-consumption-v12.0.2`;
its object URL uses the lease OID. `node_id` is a nonempty JSON string,
non-authoritative and never logged or used as identity.

The sanitized incident receipt does not contain every top-level response key;
I2 used minimal synthetic POST/GET/ref responses. These closed shapes are
explicit R3 policy, not a claim that the old receipt empirically proved all of
them. I3 must not invent or expand fields to accommodate an unexpected reply. A discrepancy permanently stops this version
before createRef, claim, history, forecast or outcome access, even if commit
upload may already have occurred.

Call this **local canonical raw bytes plus server POST/GET exact
content-addressed OID and a fixed JSON display projection**. It is not a
claim that remote raw commit bytes were downloaded. GitHub's
[Git commit API](https://docs.github.com/en/rest/git/commits) specifies JSON
fields without a raw-message trailing-LF preservation guarantee. The
[general commit API](https://docs.github.com/en/rest/commits/commits#get-a-commit)
provides diff/patch/SHA representations, while
[raw blob retrieval](https://docs.github.com/en/rest/git/blobs#get-a-blob)
concerns blobs. The no-LF projection is based on the fixed committed incident
observation; R3 does not assert a universal or permanent API guarantee. No
assumption that an unattached remote commit can be fetched is needed.

## Authorization before startup; startup before capability and POST

First validate isolated source, original full-clone cleanliness, fixed command
and local/remote auth facts completely, without issuing execution capability
or writing any artifact. All nine reserved paths must be absent. A failed
preflight may emit only a fixed safe stage/error enum to stderr. It creates no
startup, claim, ledger, report or remote mutation. Startup existence itself is
never authorization evidence.

Only after those read-only checks establish exact authorized M_A_H3, exclusively
create the fixed startup at mode `0600`; reject symlink/unsafe directory paths,
retain its FD/dev/inode identity, and append its first canonical event binding
the fully verified authorization facts. Flush/file-fsync the event and fsync
the parent directory. Only then issue execution capability and perform fresh
lease absence checks. This preserves the no-artifact-before-authorization rule
while making every remote mutation traceable before it occurs.

Startup events are finite canonical JSONL, contiguous zero-based sequence,
trusted UTC `Z` time, and SHA-256 links to the previous complete event line
including LF. Do not reuse the scientific DurableLedger algorithm that excludes
a self-hash field; the startup chain is its separately registered full-line
algorithm. Schemas permit only fixed phase/result enums, validated integer
HTTP status or null, and OIDs only after equality with the expected identity.
Never record tokens, headers, environments, arbitrary errors, server text,
request/response bodies, redirects or other uncontrolled strings. The random
32-byte nonce is a safe noncredential identifier.

Before **each** POST, append/flush/fsync an intent with phase enum, nonce,
expected OID, raw-commit SHA-256, exact request-body byte count and SHA-256.
Serialize finite canonical UTF-8 request JSON once without LF, digest those
bytes and send the same bytes. Do not let an HTTP library reserialize them.
A write/flush/fsync failure prevents the POST. A POST with no durable receipt
has unknown outcome and terminates; retain the intent without fabricating
success. Preserve every partial byte and do not fall back to another log.

Fresh exact GitHub 404 for the literal new lease ref is the only absence
predicate. At most one commit POST and one createRef POST may occur. Exact
POST OID and GET projection checks precede createRef. Only successful createRef,
its fresh exact-ref/OID reread and unchanged protected main grant the lease.
Existing/ambiguous refs, failures, unknown outcomes and partial startup
terminate this version. Neither automatic nor manual retry, resume, ref
update/delete, nonce reuse or old lease adoption is allowed. A later 404 never
restores authority.

Later clean checks recognize only this process's exclusively created fixed
startup using the same retained FD, fixed path/HEAD, device/inode, regular-file
and single-link status, safe permissions and current writer length/hash. Every
other untracked/unexpected ignored output fails. A filename, matching-looking
JSON or old file is never a cleanliness exception. The legitimate retained FD
must also not be misclassified as foreign merely because the file now exists.

After claim creation, later full-clean checks must likewise recognize only the
fixed outputs exclusively created and registered by this invocation's issued
capability. Bind each role/path to the exact repository/HEAD, the same live
capability, its creation or exclusive atomic-publication provenance, file
identity and current complete bytes or registered staging-state digest. The
legitimate claim and scientific ledger cannot be mistaken for foreign files.
A name/path allowlist without current creation provenance is insufficient;
preexisting/foreign outputs and every other untracked or unexpected ignored
entry still fail. Before claim, the other eight scientific paths are absent.

## Acyclic checkpoint, claim and ledger

After fresh lease verification, durably append a startup checkpoint and save
its fixed path, **sequence, head SHA-256, prefix byte count and prefix SHA-256**.
The immutable claim binds that already-fsynced prefix together with the frozen
scientific and new operation identities. The first scientific ledger event
binds the same prefix and the completed claim SHA-256.

Startup then appends, in order, `claim_created` with claim SHA-256,
`scientific_ledger_started` with first scientific event SHA-256, and
`history_load_intent`; flush/fsync each. Seal startup against further append
before exposing governed history. Retain its original FD solely for exact
identity/cleanliness checks through exit, then close it. Seal means no more
journal writing; it does not loosen ownership checks.

The final artifact manifest separately binds the complete sealed startup's
byte count/SHA-256 and the earlier checkpoint. Never require the claim to
contain the future complete startup digest, rewrite the claim or fixed
scientific bindings, or create a cyclic hash dependency. A crash at any edge
preserves only actual durable evidence and terminates the version. Intent
alone does not prove a request occurred; absent receipt means unknown. No
recovery may finish a line, invent a receipt or continue scoring.

## R3 authority, preservation and outcome-blind validation

R3 authority is the unique first ordinary ancestor commit whose diff **adds
the exact v3 registration path with status A**. It cannot place its own future
SHA into its bytes. Reject ambiguous adds/re-adds and rewritten history; after
binding require the same registration blob. Traverse all reachable ancestors
without path-history simplification and explicitly verify no-rename status A
against the unique ordinary source parent. Prebinding registration checks may
read the worktree only when the registration path is absent from HEAD, solely
as draft validation and never execution authority. A committed registration
without a valid ordinary ADD source is a failure, not an uncommitted draft.

At the fixed R3 tree, all six I3 paths, new auth and nine real output paths
must be absent. The shared core must already exist at the sealed hash. Later
I3 additions cannot change where registration-time absences are checked.
The root owner seals final base, `config.yaml`, old-byte preservation manifest,
registered files and synchronized status docs from their final bytes. The seal
excludes its own digest; config does not contain the future seal's digest.
No guessed commit or digest is allowed.

Existing R1/R2 registrations/auth, I2 source/tests/launcher, pure core and
requirements remain byte-identical. Only synthetic fixtures, fixed literals,
closed-form oracles, registration/config/spec bytes and immutable Git metadata
are allowed for review. During R3 registration, no runtime verifier, canonical
entrypoint or worker is invoked. I3 implementation review may execute pure
validators and simulated adapter/worker paths only in isolated synthetic or
closed-form tests. Such tests must never create a canonical production
capability/attempt, access real history or target answers, mutate a network
service, or create any registered real artifact. A mock worker on synthetic
inputs is permitted to test failure ordering; a canonical worker is not.
No new raw governed-history/target records, V12 canonical predictions/scores
or secrets are opened. Previously published V1 aggregates remain known,
without any claim of restored blindness. The fixed forecast,
scoring, bootstrap, notification and historical governance rules retain R1's
meaning. Tests must prove exact projection, malformed/wrong-LF cases,
pre-POST durability, sanitized failure evidence, one-shot counts, legitimate
FD ownership and foreign-file refusal, acyclic checkpoint binding and exact
synthetic equivalence to the old scientific behavior.

Any missing authority, statistical drift, stale review/CI, closure/byte mismatch,
projection discrepancy, unsafe startup ownership or uncertain mutation closes
execution. An operational failure with no scores cannot be fabricated into a
scientific result. All R1 scientific Reject/Archive distinctions remain intact.
