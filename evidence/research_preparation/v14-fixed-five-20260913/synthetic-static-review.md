# 五组概率合成验证辅助脚本：执行前独立静态审查

审查者：`/root/five_group_independent_math_review`，非脚本作者。

对象：`/private/tmp/v13-successor-probability-synthetic-validation-20260913.py`，实际 SHA-256 `b0a4edd72108b92f8912d5b168d0ca4351a41730185e0c4c5c9dbaf150074d26`。

**结论：PASS，0 blocker、0 major。仅批准其作为所述有限范围的合成工程验证辅助脚本进入执行准备；本审查没有执行它。** 没有任何模型结果、数值测试成功或新实验授权结论。

## 读取、执行与写入边界

静态路径只读取指定解释器身份、固定源文件、要求文件、保留的科学v1文本、固定Git配置及本地Git元数据/已固定源码blob。未发现读取历史载荷、真实目标答案、报告结果、SMTP或其他凭据的代码；没有远端命令、正式worker、canonical attempt、claim、lease或邮件调用。

所审7个项目Python源文件只含包常量、既有模型定义、纯特征、概率归一化、Draw和排名/选择函数；实际只调用 `LongFrequencyModel.predict` 与 `rank_numbers`。`RandomBaseline` 等类虽存在于旧源码文件，没有被构造或调用。脚本的7组模型输入均由明示的2040年起虚构逐日记录组成，不来自任何仓库数据。

执行前要求隔离/禁用site/禁写bytecode标志、有限环境键、固定解释器路径和哈希、数值平台、全清洁源树及源码哈希。Git使用固定命令路径、受限环境、禁止协议、禁hooks等设置；项目import白名单和来源核对辅助约束读入模块。NumPy/Pandas版本与来源另行检查；没有声称已证明完整第三方二进制闭包或抵御所有动态绕过。这个有限工程边界与脚本自己的声明一致，不能拿来代替正式运行闭包验收。

只有两个固定外部log/proof路径可由脚本创建，均排他新增、拒绝既存或符号链接，日志与proof文件同步落盘；前后重新核对源树。输出是明确标为合成工程检查的摘要，不是历史预测或研究报告替代品。异常细节只保留固定错误码或类型，不打印外部异常载荷。执行仍应由父代理核对本报告绑定的精确脚本字节和预定清洁环境。

## 科学与数学一致性

脚本直接执行固定旧概率入口，保留 `mean*N`、strength250和原归一化的顺序；对照是独立计数形成的有理数 `(49*c+1500)/(49*(N+250))`。平衡、追加一期、标签镜像及4444期极端集中/平衡构造均是预写定的合成案例，不是搜索历史表现。原生49号键、原生float、有限开区间、升序fsum质量误差≤1e-12的规则与科学稿一致，容差不随输出调整。

行列构造与独立的固定排名位置表相互核对，强制5个六数组、30个不同标签、完整Top30分区。capture IDs恰为G1–G5，旧字段只别名G1；Top-6仅保存作诊断。没有第六个获准组或根据答案重新选组的路径。

Brier、未截断binary Log Loss按标签升序使用fsum，与科学文本的算术顺序相符。公平Brier的有理数oracle正确；公平Log Loss是等价闭式参考，容差预定。十个校准箱使用明确浮点边界比较；其边界上下相邻浮点、空箱null和公平计数检查与既定规则相符。

脚本绑定保留的科学v1 `dc3e0c8d2510d982e06544d57d5a975acc37d441657365bdc2ff30b06ce53153`。独立审查已经确认科学v2 `92c8b07dcd0b02b7998ce9d0f092292a25040b6f429d7e7de833e61da25f6fac` 仅调整基准协议说明和失败阶段规则，未改变本辅助脚本涉及的概率、分组、容差、评分或校准公式。因此此处使用保留v1身份不造成数值合同冲突；它不是对v2全部状态规则的测试。

## 必须保留的验证范围限制

本脚本不实现或检验正式N≥300协调门槛，N=1明确仅检查旧底层模型。它也未覆盖全部质量容差临界相邻值、冻结/揭晓防泄漏、durable intent、缺组/capture/unknown终态、12CI或全部正式输出schema。已有合成案例通过时，只能按实际检查清单报告，不能据此宣布下一实验完整实现、完整测试或正式可运行。

这不是阻止本有限辅助验证的缺陷：脚本顶层与结果字段已经明示不是新实验实现、不是独立审查、不是最少历史协调器、不是完整运行闭包或运行授权。后续正常注册和实现仍须完成相应验收。

## 本轮实际动作

读取辅助脚本及指定旧项目源，使用标准库静态解析提取PINS，独立核对8个所列源/要求文件的SHA-256，全部匹配。未导入任何项目模块，未执行该辅助脚本或任何模型，未读取真实历史/答案/报告/凭据。只新增本外部审查报告，没有修改被审文件或仓库。
