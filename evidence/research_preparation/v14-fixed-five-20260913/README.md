# V14 preparation archive

These are exact original bytes from the source, mathematical and fabricated-fixture preparation of one proposed fixed-five-group experiment. This directory contains no real V14 historical predictions, scores, claims, lease or execution authority. The accompanying registration and its scientific/operational documents, once actually registered, define the normative experiment.

`manifest.json` pins all 16 originals by size and SHA-256. The original absolute temporary paths and old draft statuses inside those files are provenance at their creation time; they are not runtime dependencies or statements that a later registration already existed. Sources archived with `.py.txt` are inert documentary copies, not installed launchers or executable project tests. Nothing here authorizes rerunning the already finished V13 worker.

The initial long-frequency probability-source proposal, Top-30 pool, five disjoint snake groups and primary maximum-hit statistic were recorded in `pre-outcome-concept.md`; that external file reports its creation time before the V13 result. It did not inspect or freeze parameter values or source identities. Strength 250 and the exact inherited floating-point pipeline were verified and fixed in the later `source-map.md` and complete science preparation after V13 outcomes. Git publication occurs now and does not retroactively certify that timestamp or restore blind status. The integrated science and operations drafts were written after V13 outcomes; their authors' prior exposure and roles remain disclosed. No V14 outcomes informed these choices.

The original independent reviews cover the exact input drafts and mathematical/helper scope they name. They do not grant approval to the later R14 source, implementation, complete runtime or historical authorization. Fresh independent Standards and Spec reviews of the actual registration are required. Root's later helper delta review is explicitly root-authored, not a second independent review.

The helper's original environment guard stopped before project import or output creation because macOS added `__CF_USER_TEXT_ENCODING`. `synthetic-v1-failure.json` preserves the root-relayed actual traceback and its evidence limits; the failed source remains unchanged. The v2 helper allowed only that Darwin-specific variable name and used new exclusive output paths. It did not read its value or change any formula, fixture or the predeclared 1e-12 tolerance.

The v2 run actually exited zero. Its seven artificial 2040+ histories, 15 malformed probability dictionaries, exact ties/one-ULP ordering, five fixed group identities, 29 calibration boundaries and eight invalid bin inputs passed the bounded checks. The maximum observed rational error was 1.1102230246251565e-16 and probability-mass error 8.881784197001252e-16. These are engineering equality results, not lottery performance. N1 exercises only the inherited mathematical bottom layer; this run did not implement or verify the formal N>=300 coordinator, chronological I/O state machine, full runtime closure or worker.

The mathematical oracle and reviews verify closed combinatorial counts. Its first handwritten expected-count literal had an arithmetic typo and failed its assertion; the retained math-contract and review describe that preparation correction. No lottery outcome, candidate parameter or selection rule was changed in response. Later registered tests must independently verify their own implementation.

Original files remain byte-for-byte immutable, including any Markdown hard breaks, historical draft wording and evidence limitations. New explanations belong in new files. A successful local helper or a published archive never completes the research Goal.
